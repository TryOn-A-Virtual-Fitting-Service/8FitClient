export interface ModelItem {
    id: number;
    imageUrl: string;
    name: string;
    modelImageUrl: string;
  }
  
  export interface ModelImage {
    id: string;
    imageUrl: string;
    timestamp: string;
  }